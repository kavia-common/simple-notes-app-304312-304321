# simple-notes-app-304312-304321

A simple React notes application (frontend-only) that stores notes in browser `localStorage`.

- List notes with title + preview snippet
- Add/Edit/Delete notes
- Search and sort by last updated time
- Modern light theme with blue/cyan accents

Run it from `frontend_react/`:

```bash
cd frontend_react
npm start
```
